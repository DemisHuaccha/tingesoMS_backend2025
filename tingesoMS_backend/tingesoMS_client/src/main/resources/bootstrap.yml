spring:
  application:
    # Debe coincidir exactamente con tingesoMS_client.yaml en tu repo de GitHub
    name: tingesoMS_client
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:8081
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 5