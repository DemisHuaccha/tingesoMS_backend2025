spring:
  application:
    # Debe coincidir exactamente con tingesoMS_client.yaml en tu repo de GitHub
    name: tingesoMS-client
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:8888
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 5