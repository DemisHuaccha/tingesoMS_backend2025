server:
  port: 8761

eureka:
  instance:
    hostname: eureka-service
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      # Mantenemos defaultZone en camelCase para compatibilidad total
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/