server:
  port: ${PORT:${SERVER_PORT:0}}

spring:
  application:
    # Nombre de registro para que el Gateway lo encuentre vía lb://tingesoMS_loan
    name: tingesoMS_loan
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/loan_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://${EUREKA_HOST:eureka-service}:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    # Recomendado para estabilidad en la comunicación entre microservicios
    prefer-ip-address: true
