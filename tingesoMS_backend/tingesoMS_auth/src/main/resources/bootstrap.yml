spring:
  application:
    # Este nombre es el que usa el Config Server para buscar el .yaml en GitHub
    name: tingeso-auth-service 
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:8888
      fail-fast: true