apiVersion: apps/v1
kind: Deployment
metadata:
  name: tingeso-loan
spec:
  selector:
    matchLabels:
      app: tingeso-loan
  template:
    metadata:
      labels:
        app: tingeso-loan
    spec:
      initContainers:
      - name: wait-for-infrastructure
        image: busybox
        # ESPERA TANTO A MYSQL COMO AL CONFIG-SERVICE
        command: ['sh', '-c', 'until nc -z mysql-service 3306 && nc -z config-service 8888; do echo "Esperando infraestructura..."; sleep 5; done;']
      containers:
      - name: tingeso-loan
        image: demishuaccha/tingeso-loan-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: SPRING_CONFIG_IMPORT
          value: "optional:configserver:http://config-service:8888"
        - name: MYSQL_HOST
          value: "mysql-service"
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-secrets
              key: user_loan
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secrets
              key: pass_loan
        - name: EUREKA_HOST
          value: "eureka-service"

        # SONDAS DE SALUD EN EL CONTENEDOR (Correcto)
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 70
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: tingeso-loan
spec:
  selector:
    app: tingeso-loan
  ports:
  - port: 8080
    targetPort: 8080
  # SE ELIMINARON LAS SONDAS DE AQUÍ
  type: ClusterIP